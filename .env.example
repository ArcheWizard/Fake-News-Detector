# Choose which run to mount into containers
# e.g., roberta-kfr, quick-test, my-experiment
MODEL_RUN=roberta-kfr

# Subdirectory under the run containing the saved model
# Typically 'model' but can be 'model-quant' or 'model-pruned'
MODEL_SUBDIR=model
