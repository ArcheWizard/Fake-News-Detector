repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.2  # Match your requirements.txt version
    hooks:
      # Run linter
      - id: ruff
        args: [--fix]
      # Run formatter (replaces Black)
      - id: ruff-format
